{
  "name": "visitor-pdf-service",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild index.js --bundle --platform=browser --target=es2020 --outfile=dist/index.js"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1",
    "qrcode-generator": "^1.4.4"
  },
  "devDependencies": {
    "wrangler": "^4.x.x",
    "esbuild": "^0.20.0"
  }
}